//Ejemplo básico --> el bloque de código se ejecuta al menos una vez antes de comprobar la condición
let contador = 0;

do {
    console.log('El contador es: ' + contador);
    contador ++;
} while (contador < 5);

