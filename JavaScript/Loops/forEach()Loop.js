//Ejemplo aplicado del método 'forEach()'
let numeros = [1,2,3,4,5,6,7,8];

numeros.forEach(function(numero) { //numero representa cada elemento de la matriz dada
    console.log(numero * 2);
});